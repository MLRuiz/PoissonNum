%% Ejemplo de una funci\'on para probar los c\'odigos de diferencias y volumenes
 % para la aproximaci\'on de la ecuaci\'on de Poisson
function f=f(p,k)
for i=1:size(p,1)
    f(i)=-5.0*exp(2*p(i,1)+p(i,2))*exp(-k);
end
f=f(:);
